*[English version](README.md)*

# WikiPulse

Живая карта мира, на которой место загорается, пока правят статью Википедии
о нём, и рядом дашборд с аналитикой поверх истории этих правок. Обе половины
кормит один пайплайн, и этот пайплайн целиком живёт на **YTsaurus + SPYT**:
без Kafka, без отдельного хранилища под витрины, без второй системы хранения
где бы то ни было.

Смысл этого репозитория не в карте, а в форме сервиса. Это разобранный пример
того, как выглядит приложение поверх YTsaurus от начала до конца — очереди
с консьюмерами и автотримом, справочник, в который ходит потоковая джоба,
статическая таблица истории, батчевые витрины по расписанию и REST API,
который отдаёт всё это браузеру. Каждый кусок достаточно мал, чтобы прочитать
его за один присест, и у каждого неочевидного решения записана причина.

Конкретно это четыре подвижные части: обычный python-продюсер, который льёт
поток событий Wikimedia в очередь; потоковая SPYT-джоба, которая джойнит
каждую правку со справочником координат и назначает ей H3-ячейку; пара
батчевых шагов, которые архивируют очередь и пересобирают витрины дашборда;
и сервис на Spring Boot, который держит в памяти окно последних 30 минут
обогащённой очереди и сворачивает его в шестиугольники той резолюции, которая
нужна текущему зуму карты. Клиент — React SPA с двумя маршрутами.

Это честный снимок состояния, а не отполированный продукт. SPYT-джобы
намеренно наивные — `lookup_rows` на батч, а не broadcast join, — потому что
переписать их без кластера, на котором это можно проверить, означало бы
положить непроверенный Spark-код в пример, который люди будут копировать.
Известные пробелы не спрятаны, а названы в документах модулей:
`bigdata/implementation-notes.md` фиксирует, в частности, что `dict/coords`
приходится переводить в динамическую таблицу руками и что `q_enriched` пока
фактически не тримится.

## Как это работает

```text
  Wikimedia SSE (recentchange)
        │
        │  ingestor            обычный python, работает где угодно
        ▼
  {BASE}/q_raw                 очередь YTsaurus
        │
        │  spyt_enrich         потоковая SPYT-джоба на кластере:
        │                      lookup в {BASE}/dict/coords, H3-ячейка r9 на правку
        ▼
  {BASE}/q_enriched            очередь YTsaurus
        │
        ├───────────────────────────────────────────────┐
        │  archiver                                     │  поллер внутри бэкенда
        ▼                                               ▼
  {BASE}/history/t_history                        окно 30 минут в памяти
        │                                               │
        │  spyt_marts          батчевая SPYT-джоба      │  ячейки r9 сворачиваются
        ▼                                               ▼  до резолюции зума
  {BASE}/marts/trends                             GET /api/v1/hexagons/active
  {BASE}/marts/top_articles                              │
  {BASE}/marts/top_geo                                   ▼
        │                                             карта
        │  GET /api/v1/dashboard
        ▼
     дашборд
```

`{BASE}` — это `YT_BASE_PATH`, по умолчанию `//home/wikipulse`. От него
строятся все пути проекта: путей кластера в исходниках не зашито. Двумя
батчевыми шагами слева управляет `scheduler` — он раз в пять минут гоняет
`archiver`, а затем `spyt_marts`.

## Структура репозитория

```text
.
├── frontend/   React-клиент: живая карта и дашборд
├── backend/    REST API на Java / Spring Boot / Maven
├── bigdata/    пайплайн: продюсер, SPYT-джобы, создание таблиц, заливка артефактов
├── deploy/     контейнеры, Caddy, конфигурация выкладки
├── docs/       архитектура, контракты, runbooks
└── setup/      как настроить машину под YTsaurus и SPYT
```

## Что нужно

| Что | Версия | Для чего |
|---|---|---|
| JDK | 17 | `backend` — `java.version` в `backend/pom.xml` |
| Node.js | `^20.19` или `>=22.12` | `frontend` — требование Vite 8 |
| pnpm | закреплён полем `packageManager` | `frontend`, ставится через corepack |
| Python | `>=3.12` | `bigdata` — `requires-python` в `pyproject.toml` |
| Docker с плагином compose | — | `deploy` |
| Кластер YTsaurus со SPYT | — | всё, кроме профиля `mock` |
| Ключ JavaScript API Яндекс Карт | — | чтобы карта вообще отрисовалась |

Ничего больше, чтобы увидеть продукт, не нужно: первый шаг ниже работает без
кластера и без ключа карт.

## Быстрый старт

### 1. Весь продукт вообще без кластера

У бэкенда есть профиль `mock`, который отдаёт настоящие данные из фикстур.
Запустить его:

```bash
cd backend
SPRING_PROFILES_ACTIVE=mock ./mvnw spring-boot:run
```

и во втором терминале клиент:

```bash
cd frontend
pnpm install
pnpm dev
```

Открыть <http://localhost:5173/map>. Dev server проксирует `/api` на
`http://localhost:8080`, поэтому бэкенд должен быть поднят первым.

`YT_PROXY` и `YT_TOKEN` здесь не нужны: бины, которые их читают, помечены
`@Profile("yt")`, и плейсхолдеры на `mock` не резолвятся вовсе. А вот
`SPRING_PROFILES_ACTIVE` нужен: профиль по умолчанию — `yt`, а он ходит
в кластер по RPC.

Данные при этом не выдуманные. Живая карта крутит 730 настоящих правок
Википедии, дашборд отдаёт три настоящие витрины — всё снято через публичный
API боевого стенда команды 30 августа 2026 года и лежит в
`backend/src/main/resources/fixtures/`. На старте вся выборка сдвигается так,
чтобы последняя правка легла ровно на *сейчас*: окно снапшота совпадает
с собственным получасовым окном карты, и карта полная сразу, а не заполняется
полчаса. Дальше правка проигрывается раз в две секунды.

Одна вещь в моке синтетическая, и знать про неё стоит до того, как читать
что-то по карте: координат правок в фикстуре нет — справочник координат через
публичный API недоступен. `MockPoller` назначает правке ячейку по хешу её
языкового раздела поверх настоящих значений `h3_parent` из фикстуры дашборда.
Правка настоящая и ячейка настоящая, а связь между ними — нет.

Чтобы вместо экрана ошибки увидеть настоящие тайлы карты, добавь
`YMAPS_API_KEY=<ключ>` в команду бэкенда — см. [Ключ Яндекс Карт](#ключ-яндекс-карт).

### 2. На настоящем кластере YTsaurus

```bash
export YT_PROXY=https://<proxy-host>
export YT_TOKEN=<токен>
export YT_BASE_PATH=//home/wikipulse

cd bigdata
pip install -e .

init-tables        # каталоги, очереди, консьюмеры, автотрим, история, витрины
upload-artifacts   # собрать и залить bigdata.zip, h3.zip и скрипты джоб
ingestor           # Wikimedia SSE → q_raw
```

`upload-artifacts` — не опциональное удобство и не разовая мелочь: без
`bigdata.zip` и `h3.zip` в `{BASE}/lib` любая SPYT-джоба падает на кластере
с `ModuleNotFoundError: bigdata`. Справочник координат, потоковая джоба,
архиватор, витрины и шедулер — в [`bigdata/README.md`](bigdata/README.md),
в том порядке, в котором их нужно запускать.

Когда таблицы наполнены, бэкенду профиль не нужен — `yt` и так по умолчанию:

```bash
cd backend
YT_PROXY=$YT_PROXY YT_TOKEN=$YT_TOKEN YT_BASE_PATH=$YT_BASE_PATH \
  YMAPS_API_KEY=<ключ> ./mvnw spring-boot:run
```

Бэкенд ходит в YTsaurus по RPC на порт 9013, который из домашних сетей обычно
закрыт; с облачной ВМ работает. Python-часть ходит через HTTP-прокси и
работает откуда угодно.

### 3. Всё в контейнерах

```bash
cd deploy
cp .env.example .env     # заполнить YT_PROXY, YT_TOKEN, DOMAIN, ACME_EMAIL
docker compose up -d --build
```

Четыре контейнера: бэкенд, ингестор, шедулер и Caddy. SPYT-джобы работают на
кластере и в compose не входят, поэтому `upload-artifacts` должен быть уже
выполнен против этого кластера — иначе шедулеру нечего будет считать. Caddy
обслуживает ровно тот хост, который назван в `DOMAIN`, и получает на него
сертификат сам; `DOMAIN=http://localhost` выключает HTTPS, чтобы посмотреть
стенд локально. Всё остальное — TLS с файловым сертификатом, `extra_hosts`
конкретного стенда, заголовки безопасности, место на диске, уже собранные
грабли — в [`deploy/README.md`](deploy/README.md).

## Конфигурация

Всё, что сервис берёт снаружи, приходит переменными окружения: адресов
кластера, токенов и путей в Cypress в исходниках нет. Полная таблица — какая
переменная обязательна, какие значения по умолчанию, кто что читает — это
раздел «Переменные» в [`deploy/README.md`](deploy/README.md), а
[`deploy/.env.example`](deploy/.env.example) — её заполняемый шаблон.

Форк меняет две вещи: кластер, в который он ходит (`YT_PROXY`, `YT_TOKEN`),
и корень, в который он пишет (`YT_BASE_PATH`).

Workflow выкладки `.github/workflows/deploy.yml` копирует исходники на сервер
и запускает там compose. **В форке он молчит:** пока переменная репозитория
`DEPLOY_HOST` не задана, job не запускается, и пример не пытается выложиться
на чужую машину. Свой хост настраивается переменными и секретами репозитория,
либо файл удаляется.

## Ключ Яндекс Карт

Карта — единственная внешняя зависимость, которую нельзя обойти: JavaScript
API Яндекс Карт 3.0 без ключа не отрисовывается. Два свойства этого ключа
удивляют людей, и оба стоят вечера, если выяснять их отладкой:

- **Ограничение по HTTP Referer обязательно.** Ключ без заданного Referer не
  работает нигде, а ошибка приходит изнутри API и звучит невнятно. В поле
  кладётся только хост — без схемы и без суффикса `/*`, — и голый IP-адрес там
  не принимается, нужен домен; для локальной разработки годится `localhost`.
- **Бесплатный тариф — 100 загрузок карты в сутки**, и каждая загрузка страницы
  тратит одну. Перезагрузок во время разработки хватает, чтобы её исчерпать.

Ключ не секрет: фронтенд получает его в рантайме из `GET /api/v1/config`,
и в браузере он виден. Защищает его только ограничение по Referer. Бэкенд
читает его из `YMAPS_API_KEY`; если переменной нет, API вернёт пустую строку,
карта покажет экран «Не удалось загрузить карту» с кнопкой повтора, а
остальная страница продолжит работать.

Подробности, включая то, какой именно тип ключа заказывать, — в
[`frontend/README.md`](frontend/README.md).

## Документация

- [`docs/README.md`](docs/README.md) — индекс всего остального.
- [`docs/03-contracts/`](docs/03-contracts/) — границы: схемы таблиц YT и REST
  API. Если код разошёлся с ними, неправ код.
- [`docs/05-runbooks/local-setup.md`](docs/05-runbooks/local-setup.md) —
  длинная форма первого шага быстрого старта.
- [`setup/spyt-env.md`](setup/spyt-env.md) — как настроить машину на кластер
  YTsaurus и запустить первую SPYT-джобу.
- `bigdata/implementation-notes.md` и
  [`docs/02-architecture/frontend-implementation-notes.md`](docs/02-architecture/frontend-implementation-notes.md)
  — почему код написан именно так. Комментариев в коде проект не держит,
  причины живут в этих документах, привязанные к файлу и символу.

Документация в `docs/` ведётся по-русски. Этот README — единственный документ,
который поддерживается на двух языках; задачи и pull request'ы принимаются
на любом из них.

## Лицензия

Apache-2.0 — см. [LICENSE](LICENSE) и [NOTICE](NOTICE). Заголовков лицензии
в исходных файлах нет, их покрывают файлы в корне.

Компоненты, которые проект не просто использует, а распространяет дальше —
колесо h3-py, заливаемое на кластер, шрифт Manrope внутри сборки фронтенда,
экспорт стиля Яндекс Карт — перечислены в [THIRD-PARTY.md](THIRD-PARTY.md),
а тексты лицензий, отличных от Apache-2.0, — в
[THIRD-PARTY-LICENSES.txt](THIRD-PARTY-LICENSES.txt). Обычные зависимости
Maven, pnpm и pip там не перечисляются: они объявлены в `backend/pom.xml`,
`frontend/package.json` и `bigdata/pyproject.toml`.

Как участвовать: [CONTRIBUTING.md](CONTRIBUTING.md).
Как сообщить об уязвимости: [SECURITY.md](SECURITY.md).
