/**
 * Окно живой карты в минутах. Дублирует `app.live.window-minutes` бэкенда:
 * контракт окно в ответе не отдаёт, общего источника нет. Меняется окно
 * на бэкенде — правится и эта константа.
 */
export const LIVE_WINDOW_MINUTES = 30;
